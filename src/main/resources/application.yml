server:
  port: 8081

spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/orders?sslMode=DISABLED&tlsVersion=TLSv1.2
    username: root
    password: meomeo123
    pool:
      initial-size: 5
      max-size: 20
      max-idle-time: 30m
      validation-query: SELECT 1
    properties:
      sslMode: DISABLED
      useSSL: false
      requireSSL: false
      allowPublicKeyRetrieval: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    org.springframework.r2dbc: DEBUG
    io.r2dbc.mysql: DEBUG
    org.springframework.data.r2dbc: DEBUG
